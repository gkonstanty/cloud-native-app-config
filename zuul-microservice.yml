server:
  port:
    8760

# eureka
eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8762/eureka
  instance:
    preferIpAddress: true


endpoints:
  restart:
    enabled: true
  shutdown:
    enabled: true
  health:
    sensitive: false
